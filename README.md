# This is the task 02 solution

## This contains 2 folders
- `frontend` - contains the frontend code
- `backend` - contains the backend code

## How to run the code
- `cd frontend`
- open `index.html` in browser
- `cd ../backend`
- `npm install`
- `npm start`
